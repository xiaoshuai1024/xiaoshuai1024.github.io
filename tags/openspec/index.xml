<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title>OpenSpec - Tag - 1024 工程笔记</title><link>https://xiaoshuai1024.top/tags/openspec/</link><description>OpenSpec - Tag - 1024 工程笔记</description><generator>Hugo -- gohugo.io</generator><language>en</language><lastBuildDate>Sat, 29 Aug 2026 02:07:34 +0800</lastBuildDate><atom:link href="https://xiaoshuai1024.top/tags/openspec/" rel="self" type="application/rss+xml"/><item><title>OpenSpec 和 Superpowers 别二选一,它们管的是两件事</title><link>https://xiaoshuai1024.top/posts/ai-dev-openspec-superpowers-workflow/</link><pubDate>Wed, 05 Aug 2026 09:00:00 +0800</pubDate><author>Author</author><guid>https://xiaoshuai1024.top/posts/ai-dev-openspec-superpowers-workflow/</guid><description><![CDATA[OpenSpec 装上了，Superpowers 也装上了，项目照样乱。
打开项目目录，openspec/changes/ 和 docs/superpowers/plans/ 两个目录都在，真要让 AI 动手，它还是直接改、改完不归档，下次那批约定又得从头再来。你手上两套工具，等于两套都只装了没跑。
还有一种更隐蔽的误区：以为两套都装上了，就是「规格化开发」了。结果只是多出一堆没人读的文档，AI 的产出还是无规格、无计划的状态。
问题不在工具，在于没搞清这两套东西管的是两件不同的事。这两套是唯一同时放进我日常流程的规格化工作流，反复迭代的视频管线、公众号发布、站内搜索都是它们管出来的。核心分工是：OpenSpec 管「一次变更的规格」，Superpowers 管「一个项目的计划」。 粒度不同、产物不同、驱动方式不同，配起来才完整。
为什么一个规格工具不够 先看 OpenSpec 解决什么。它把一次变更固化成 change，放进仓库的 openspec/changes/&lt;名字&gt;/，里面是四件套：proposal.md（为什么改、改什么、影响什么）、specs/&lt;能力&gt;/spec.md（系统必须做什么，增量规格）、design.md（怎么做、权衡了什么）、tasks.md（按什么顺序做）。六个 skill 组成流水线：explore 想清楚 → propose 生成四件套 → apply 照 tasks 施工 → sync 预览合并效果 → update 回炉修订 → archive 归档并合并进主规格。
这套东西管到的最小单位是一次变更。它把「加个缓存」「加个封面自动上传」这种改动定义得清清楚楚，AI 照着 spec 施工，改完归档，能力攒进主规格。我在 /posts/ai-dev-openspec-spec-driven/ 里用一条加缓存的改动把六个 skill 走了个遍，这里不重复。
但一整个项目目标不是 change 的粒度。「把公众号自动发布做出来」这个目标，涉及账号对接、定时发送、封面上传、失败重试、群发通知，拆开是几十个变更。OpenSpec 能把每一个变更定义明白，却管不了这几十个怎么排、阶段怎么分、跨变更的依赖是什么、整个目标收敛没有。那不是变更规格层的事。
再看 Superpowers 管什么。仓库里 docs/superpowers/plans/ 下每个 plan 长这样：Goal（定目标）、Architecture、Tech Stack、Spec 引用、文件结构表、checkbox 任务清单。由 superpowers:subagent-driven-development 按计划逐条执行，勾完一个推进一个。它管到的最小单位是一个项目的落地：目标怎么拆、任务怎么排、进度怎么跟踪。
但 plan 不承担变更规格。plan 里写「做站内搜索」，不会细到每个接口的验收场景。这些细节天然该落在变更规格层，而不是塞进项目计划。
两套粒度不同，一个管项目计划（大），一个管变更规格（小）。只用任一个，就有一层没人管：只上 OpenSpec，项目级的目标没人拆解跟踪；只上 Superpowers，变更级的行为没人定义验收。这不是二选一，是缺一半。]]></description></item><item><title>装完 OpenSpec 的 skill 不会用,等于往仓库扔白纸</title><link>https://xiaoshuai1024.top/posts/ai-dev-openspec-spec-driven/</link><pubDate>Sat, 01 Aug 2026 09:00:00 +0800</pubDate><author>Author</author><guid>https://xiaoshuai1024.top/posts/ai-dev-openspec-spec-driven/</guid><description><![CDATA[装上 OpenSpec 的 skill 只是开始,真正的问题是怎么用对它们。
我见过太多人装完 OpenSpec 的 skill 就放着——知道有 /opsx:propose 建变更,剩下五个 skill 全在吃灰,或者用错时机。skill 装得全不等于会用,就像买了全套工具,不等于每把都摸过。
我在「前端 → 全栈 → AI 开发」这条线上一路走过来,OpenSpec 的 skill 是我唯一真正放进日常流程的规格驱动工具。这篇文章就把这六个 skill 按真实使用顺序逐个讲透:每个解决什么问题、产出什么、什么时候用、怎么和前后衔接。
结论是:这六个 skill 不是六个独立工具,是一条流水线。 想法进来,经过 explore → propose → update → apply → sync → archive 六道工序,变成主规格里沉淀下来的能力。每一步的产物,都是下一步的输入。
下面我用一条真实的改动走完整条流水线:给一个查询功能加缓存。它不涉及任何具体业务,就是个最常见的编程场景——查询太慢,想加一层缓存。这条改动小到看得懂,又完整到每一步 skill 都有对应动作。
先认识四个产出物 讲 skill 之前,得先认识它们操作的对象。OpenSpec 把一次改动固化成一个 change,里面是四个文件,放进仓库的 openspec/changes/&lt;名字&gt;/ 目录。四件套是流水线反复操作的工件:
proposal.md——为什么改、改什么、影响什么,相当于立项报告 specs/&lt;能力&gt;/spec.md——系统必须做什么,是增量规格,只声明本次改的行为 design.md——怎么做、权衡了什么,是决策留痕 tasks.md——按什么顺序做,是执行计划 四件事混在一份文档里,就是一份没人能维护的巨型需求书;拆开,每一件都能单独演进、单独 review、单独归档。四个产出物是你和 AI 之间的合同,后面的 skill 全部围着它们转。
第一个:explore——想清楚再动手 流水线的入口不是建变更,是 explore。它是个思考模式,不写代码、不建 change,只做一件事:把模糊的想法摊开,变成可以写进 proposal 的共识。
什么时候值得先 explore?]]></description></item><item><title>你只想加个缓存，AI 想送你一个新系统：先写计划再动手</title><link>https://xiaoshuai1024.top/posts/ai-plan-first-cache-story/</link><pubDate>Sat, 29 Aug 2026 02:07:34 +0800</pubDate><author>Author</author><guid>https://xiaoshuai1024.top/posts/ai-plan-first-cache-story/</guid><description>问你一个问题：为什么你让 AI 写代码，它越写越偏？我拿一个最常见的场景给你复盘。需求只有一句：需求只有一句：「查询太慢，加个缓存。」话音刚落，AI 已经把重构方案写到一半了——它它抽象出一层通用的缓存管理器，引入了消息中间件做失效通知，顺手把查询层的目录结构也重新规划了一遍，末尾还附了一份三阶段的迁移计划和一份技术选型对比表。你只想加个缓存，它想送你一个新系统。
这种跑偏有它的机制原因：模型对「帮你想全」有天然倾向，需求里的每个空白都会被它用「常见做法」填上。空白越多，填出来的东西离你的本意越远。这种热情的破坏力在于，它的每一步单看都合理，连起来是一场没有人要的改造，在你只给了一句需求，却期待它读心。这句话里至少藏着四个没回答的问题：缓存的粒度是查询结果还是整页？数据多久变一次？缓存穿透要不要处理？查询分布在几个实例？每个答案都指向不同的方案，而你一个都没给。AI 不会停下来问，它会把不确定全部猜成默认值，然后高速地把猜出来的东西建完。
解法是把顺序反过来：动手之前，先让它把话说清楚。这套流程我用了几个月，覆盖了从博客工具链到业务系统的各种改动，核心是四步——explore 摊开问题、propose 生成四件套、apply 照规格施工、archive 归档留档。这套流程跑顺之后的最大变化，是每个改动都有据可查：对的地方能指到哪条规格，错的地方能定位到哪一步走偏，改起来有明确靶子。整条流程没有一步依赖运气，也没有一步省掉判断。
先立一条总纲，后面全部展开都挂在它上面：规格是合同，不是文档。文档写完给人看，看过就过期；合同是给人和 AI 共同执行的验收基准，谁签字谁负责兑现。四步流程，本质就是把这份合同从酝酿、签订、履行到归档的完整生命周期。
一、第一步 explore：想清楚，顺便问掉一半需求 explore 是个思考环节，不写代码、不建文档，目标只有一个：把模糊的想法摊开，变成可以立项的共识。它的做法是读代码、画结构、列问题——把「查询太慢」落到「到底是哪一层慢、调用链什么样」，问题就从一句抱怨变成一张图。
explore 的动作路径很具体：让它读相关模块的代码、画出调用链、列出待决策问题。它不写实现，但读代码不算越界——把事实摸清正是这一步的全部产出。那四个悬而未决的问题，就是在这一步被逐个逼出答案的。第一问，缓存的粒度选查询结果还是整页，决定了改动的边界；第二问，数据变化频率决定失效策略选过期时间还是主动失效；
第三问，要不要处理穿透和雪崩，两种答案的实现复杂度差一个量级；第四问，服务部署在单机还是多实例，决定进程内缓存会不会各自为政。四问问完，「加个缓存」从一句愿望变成一份带边界的技术决策，每个选项后面都站着一个你亲手给过的理由，别让一个默认选择替你拍板——拍板权从一开始就该在你手里。顺带一提，这四个问题的答案没有标准答案，只有「你的系统的答案」——照抄别人博客的缓存方案翻车的案例，大多是把别人的答案抄给了自己的问题。这四个问题的答案没有对错高低，但每个答案都会把方案推向不同方向。四个答案凑齐，方案的骨架基本就定了；凑不齐，后面的每一步都在替这句模糊的需求还债。想清楚，四件套就好写；不想，规格里每个场景都得靠猜。我印象最深的一次：同样是加缓存，一次探讨完发现查询每天只有几十次，命中率撑不起缓存的维护成本，需求当场作废——那一刻的如释重负，比任何一次顺利上线都难忘。省下的不只是编码时间，还有往后每一次维护这段缓存时要重新理解它的脑力。
这一步还有个隐藏价值，我称之为最省钱的一步：它经常帮我问掉一个变更。摊开一算，查询频率低到缓存根本攒不住热度，收益覆盖不了引入的成本，直接不做了。一个需求在最便宜的阶段死掉——什么都不用改、不用测、不用回滚，血赚。动手之前最贵的一步是「没想清楚就动手」，explore 就是给这一步上保险。探不明，别立项。这话反过来说更硬：探明了才发现压根不值得做，是这个流程里回报率最高的时刻——你用半小时的对话，省下了几天的实现加无止境的维护。被问掉的需求不丢人，硬着头皮做上线才是事故的开始。
和 AI 一起做探讨还有一个容易忽略的收益：它把「我以为我想清楚了」变成「我真的想清楚了」。人脑里的方案都是压缩过的，摊开的过程就是解压缩，解出来的细节里总有几处经不起追问——而这些细节，正是 AI 自由发挥的温床。
二、第二步 propose：一次交出四件套 想清楚了就立项，AI 一次生成四份文件。先把缓存方案的结论摆出来，方便对照：查询结果级缓存、带过期时间的被动失效、暂不处理穿透、多实例共享一份集中式缓存。基于这个结论，它把探索结论固化成四份文件，构成你和 AI 之间的合同：proposal 管为什么改、改什么、明确不做什么；spec 管系统必须做什么，是行为契约；design 管怎么做、为什么这么做，是决策留痕；tasks 管按什么顺序施工，是排期表。
四份里最讲究的是 spec，它最容易写错，也最值钱。正确的写法是只写行为，不写实现，用固定的条件加结果格式：
### Requirement: 缓存查询结果 #### Scenario: 命中缓存 - WHEN 查询键在缓存中存在 - THEN 返回缓存值，不执行底层查询 #### Scenario: 缓存未命中 - WHEN 查询键不存在 - THEN 执行底层查询，并把结果写入缓存一个场景就是一个最小验收用例：AI 实现完，你照着场景逐条测，能过就是改对了。同一份规格，是指令，也是试卷。最容易犯的错是把实现细节塞进去——「用哪个缓存组件、调什么命令」那是姿势不是行为，写进去有两个坏处：AI 照着字面抄，你换存储它不会跟着变；你验收时核对的是姿势，效果反而没人看。规格只写做什么，怎么做留给设计和代码。这条分界线画清了，验收才有稳定的锚点：你测的是行为，不改技术栈它就成立。
另外三份各有一个要点。proposal 的灵魂在不做什么那一节。为什么改、改什么写起来顺手，真正起约束作用的是明确不做什么：写清「不做整页缓存」「不处理跨节点一致性」，才能拦住 AI 自作主张扩大战场。design 的灵魂是理由而非结论：「选了集中式缓存，因为查询分布在多个实例，单机会各自为政」——理由比结论值钱，下次接手的人靠理由理解系统，不靠结论。tasks 的灵魂是把摸清事实排在最前面：第一步不是写代码，是确认查询入口在哪、有哪些调用方、数据变更怎么通知失效。AI 编程里最贵的事故，都是没搞清事实就动手酿成的。四份文件还有一条依赖链：先立边界，再写契约，再谈权衡，最后排顺序。顺序反了——先想怎么做再想为什么——写出来的每份文件都在跟其他三份打架。</description></item><item><title>改一行代码别上重型流程：OpenSpec 和 Superpowers 我怎么选</title><link>https://xiaoshuai1024.top/posts/openspec-vs-superpowers/</link><pubDate>Tue, 11 Aug 2026 19:00:00 +0800</pubDate><author>Author</author><guid>https://xiaoshuai1024.top/posts/openspec-vs-superpowers/</guid><description>用 Claude Code 做全栈开发，代码不是难点，流程的轻重才是。
一个项目跑下来，你会遇到两种活：一种是改一行 SQL、修一个测试过时，十分钟就能验证完；另一种是跨前端、BFF、后端、E2E 四个系统的新功能，牵一发动全身。如果这两种活用同一套流程，要么单点修复被一堆文档拖慢，要么跨系统功能没有契约约束，改着改着就乱了。
我在低代码平台项目里同时用了两套流程：OpenSpec 管轻的，Superpowers 管重的。这轮真实开发我跑了五个 OpenSpec change 和一个 Superpowers plan，下面把过程和判断标准写清楚。
OpenSpec：给单点修复一个轻量闭环 OpenSpec 是一个 spec-driven 的轻量变更治理工具。核心想法很简单：每个改动都是一个 change，走 propose（提议）→ apply（实现）→ archive（归档）三步闭环。
它的配置文件里写死了适用边界：单个 change 不超过 3 个文件、不跨子系统、不超过 5 个 task。超过这个边界，它自己会提醒你&amp;quot;考虑用 Superpowers&amp;quot;。
这轮我用 OpenSpec 跑了五个 change，全是测试和缺陷修复：
change 改了什么 为什么走 OpenSpec engine 单测修复 4 个测试文件同步 API 重构 单子系统（engine），纯测试 backend 集成测试 1 个测试加 @ActiveProfiles 单子系统（backend-java） form 创建 500 修复 1 行 fieldSchema 默认值 单文件，缺陷修复 ui test alias 1 个 vite.config 补 alias 单文件，配置 BFF submit formId 1 个 route 加一行注入 单文件，BFF 转发 每个 change 的过程都一样：openspec new change 建目录，写 proposal（为什么改）、design（怎么改）、tasks（分几步），然后 apply 改代码、跑测试验证，最后 archive 归档。整个过程在一个对话里闭环，不需要拆分会话。</description></item></channel></rss>