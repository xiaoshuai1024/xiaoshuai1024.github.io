<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title>Transformer - Tag - 1024 工程笔记</title><link>https://xiaoshuai1024.top/tags/transformer/</link><description>Transformer - Tag - 1024 工程笔记</description><generator>Hugo -- gohugo.io</generator><language>en</language><lastBuildDate>Wed, 16 Sep 2026 11:00:00 +0800</lastBuildDate><atom:link href="https://xiaoshuai1024.top/tags/transformer/" rel="self" type="application/rss+xml"/><item><title>循环Transformer会让GPT-6 Astra藏起思维链吗？把这场架构争议拆开看</title><link>https://xiaoshuai1024.top/posts/gpt6-astra-looped-transformer/</link><pubDate>Wed, 16 Sep 2026 11:00:00 +0800</pubDate><author>Author</author><guid>https://xiaoshuai1024.top/posts/gpt6-astra-looped-transformer/</guid><description>这台模型为什么在实际任务上体感提升有限，可以看我之前写的能力分层分析；本文只聊架构争议本身。
GPT-6 Astra 发布前的两天，The Information 援引内部消息扔出一颗雷：Astra 用了一种叫 recurrent depth（循环深度）的设计，也就是 looped transformer（循环 Transformer），而它「会让模型的部分乃至全部推理过程变得不可见」。模型上线后争议越滚越大，逼得 OpenAI 首席科学家 Jakub Pachocki 亲自出面，说他想避免「一场由混乱的报道引发的、奔向不可监控性的竞赛」，并强调当前前沿模型的计算图深度与 GPT-4 的差距仍在两倍以内。
这句话信息量不小，但被各家转述得语焉不详。Sebastian Raschka（《Build a Large Language Model (From Scratch)》的作者）随后写了一篇长文，把技术细节摊开讲了一遍。这篇是我读完他的原文和相关论文后的复述与整理。结论先给出来：循环 Transformer 大概率不是思维链变短的元凶，更合理的解释是模型变强了，不需要那么长的草稿纸。
循环 Transformer 到底是什么 循环 Transformer 的定义可以这样给：让中间表示两遍或多遍地通过同一批 Transformer 块（常规架构只过一遍），每一遍复用同一套权重。
拿今年 7 月发布的开放权重模型 Nanbeige4.2-3B 做例子最直观。它的架构看起来就是个常规 Transformer：输入经过 token 化和嵌入之后，穿过 22 个 Transformer 块，每块有自己的权重（块内部是什么，可以看QKV 矩阵级拆解，这篇不展开）。区别在于多了一条绕回栈开头的路径：第一遍走完后，隐藏状态被送回这同样的 22 个块再来一遍。
把计算过程展开看，相当于 44 次块应用。但和真正拥有 44 个独立块的模型不同，第 23 次块应用用的是块 1 的权重，第 24 次用的是块 2 的权重，依此类推。有效深度从 22 提到 44，参数量却不用翻倍，这就是全部诀窍。
这个思路不新。2018 年的 Universal Transformer 论文就在做类似的事，只不过它反复应用的是单个 Transformer 块，没有用一整个栈。</description></item><item><title>3 份拷贝看懂 QKV：Transformer 1750 亿参数拆到矩阵这一层</title><link>https://xiaoshuai1024.top/posts/transformer-matrix-internals/</link><pubDate>Wed, 26 Aug 2026 09:39:00 +0800</pubDate><author>Author</author><guid>https://xiaoshuai1024.top/posts/transformer-matrix-internals/</guid><description>把 Transformer 整张网络摊开看，里面没有循环、没有卷积，没有任何称得上「结构」的东西——矩阵乘法、加法、softmax，三种运算翻来覆去叠几十层，就是 GPT 这类模型的全部。一个能写代码、改 Bug、陪你聊到深夜的程序，骨架朴素到让人愣神。
QKV 是另一处被讲玄学的地方。评论区一半人卡在「QKV 到底是什么」，我当年也卡过，卡住的原因只有一个：总想把它当成高深概念去背。把话说白就通了——QKV 不是概念，是同一个矩阵乘了三个权重矩阵得到的三份拷贝。
这篇我把一个 token 从输入到输出的完整旅程走一遍，每一步都落到矩阵形状上。看完你不光能在脑子里徒手推演整张网络，还能拿到开头那个反差的答案：奇迹不在运算本身，在运算的堆法和次数。
一、先看全景：数据在 Transformer 里走一趟 设一段输入有 n 个 token，每个 token 被表示成 d 维向量（GPT-3 小版本里 d 取 12288，教学场景常取 512）。整句话就是一个 n×d 的矩阵，从现在起你只需要盯着这个矩阵的形状变化。
它先过嵌入层拿到初始值，加上位置编码，然后进入编码器堆叠的 N 层结构。每层做三件事：自注意力（token 之间互相看一眼）、残差连接加归一化（把结果稳定地传下去）、前馈网络（每个 token 自己再深挖一次）。三层做完进下一层，N 层之后从顶端取输出。
走到嵌入层之前还有一步：分词。模型不认识字，只认识 token id。主流方案是 BPE（字节对编码）：从单个字符开始，统计语料里最常出现的相邻对，反复合并成新词元，直到词表达到目标规模。中文一个常用字多半是独立 token，英文一个长单词可能拆成两三段，数字、代码、标点各有各的拆法。「unbelievable」可能是 un+believ+able 三块，每块查嵌入表得到一个向量。分词质量直接影响一切：同样的文本，分词差劲的模型等于戴着一副度数不对的眼镜读书，而且没有「词汇必须成词」的硬约束，模型对拼写错误、混排中英的容忍度反而更高。GPT 系列词表约五万，LLaMA 系列十几万（多语言更友好），这个数字在第七节算参数量时还会回来。
一张图记住形状就够了：任何时刻数据都是 n×d，变的只是内容。注意力层让 token 之间交换信息，FFN 层让每个 token 独自消化信息，两者交替，这就是 Transformer 的全部节奏。
训练和推理时这张图的走法不一样。训练是「整句并行」：n 个 token 一次性送进去，掩码保证第 t 个位置只能看见前 t-1 个，然后每个位置同时算一遍「下一个词预测的对错」，效率极高。推理是「逐字串行」：生成一个词，把它拼回输入再算一遍，你在对话框里看到模型一个字一个字往外蹦，就是这一轮轮矩阵乘法在跑。同样生成 100 个 token，训练时一遍前向就处理完的上百个位置，推理要重复一百次前向——这就是为什么推理贵，也是后面 KV cache 这类优化存在的理由。</description></item></channel></rss>